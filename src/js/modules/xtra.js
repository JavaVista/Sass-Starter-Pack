/* A module is just a file. One script is one module. As our application grows bigger, we want to split it into multiple files, so called “modules”. A module usually contains a class or a library of functions. */

export const lifeMeans = () => {
	let journey = (11 + 3) * 3;
	journey + console.log('Life means');
	return journey;
};

